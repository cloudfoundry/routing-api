#!/bin/bash

set -e -u -x

go get -u github.com/kisielk/errcheck

errcheck ./..
go vet ./...
# running -p with consul causes test setup to fail.
# TODO: fix how we run consul for test setup
ginkgo -r -failOnPending -randomizeAllSpecs -race "$@"
